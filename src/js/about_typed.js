import Typed from 'typed.js';

if (window.matchMedia("(min-width: 821px)").matches) {
  var typed = new Typed('#typed-1', {
    showCursor: false,
    stringsElement: '#string-1',
    smartBackspace: true,
    typeSpeed: 4,
  });

  var typed = new Typed('#typed-2', {
    showCursor: false,
    stringsElement: '#string-2',
    smartBackspace: true,
    typeSpeed: 1,
  });
  var typed = new Typed('#typed-3', {
    showCursor: false,
    stringsElement: '#string-3',
    smartBackspace: true,
    typeSpeed: 1,
  });
  var typed = new Typed('#typed-4', {
    showCursor: false,
    stringsElement: '#string-4',
    smartBackspace: true,
    typeSpeed: 1,
  });
  var typed = new Typed('#typed-5', {
    showCursor: false,
    stringsElement: '#string-5',
    smartBackspace: true,
    typeSpeed: 1,
  });
  var typed = new Typed('#typed-6', {
    showCursor: false,
    stringsElement: '#string-6',
    smartBackspace: true,
    typeSpeed: 1,
  });
}